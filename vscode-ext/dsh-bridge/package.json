{
  "name": "dsh-bridge",
  "displayName": "DSH Bridge",
  "description": "Bridges this code-server instance to a DeepSeek Harness host: receives ACP-style session updates (file edits with before/after diff), enforces follow mode and file locks.",
  "version": "0.1.0",
  "publisher": "dsh",
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "dsh-bridge.reconnect",
        "title": "DSH Bridge: Reconnect"
      }
    ]
  }
}
